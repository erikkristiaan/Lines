{
    "version": "2.0.0",
    "configurations": [
        {
            "type": "",
            "name": "Debug",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}